# Normalise line endings so a Windows checkout doesn't commit CRLF into a repo
# that Linux CI, Grafana's plugin validator, and reviewers will read.
* text=auto eol=lf

# Binary / generated files git must not touch.
*.png binary
*.jpg binary
*.svg text eol=lf
package-lock.json -diff linguist-generated
go.sum -diff
